# Engineering Notes

## 2026-09-11

- Complete branch-diff review found no actionable issues: model IDs and snapshot dates match current official documentation, the existing GPT Image request path already handles the 2.5 aliases, and the diff contains no credential-like values.
- Committed the repository instructions, decision ledger, and current GPT Image 2.5 model support together as `feat: support current OpenAI image models`; the ignored per-user extension, settings, and credential remain local-only.
- Verification completed: the app started and compiled successfully; its own routes returned valid `gpt-4o-mini-tts` audio, transcribed that audio correctly with `gpt-4o-transcribe`, and generated a low-quality smoke-test image with `gpt-image-2.5-flare`. The Speech Recognition 1.1.0 manifest was also served successfully.
- Installed the root and `tests/` dependency trees from their lockfiles. All 315 unit tests passed, the changed image-generation file passes ESLint, and both dependency trees are complete. The repository-wide lint command remains red only because the pre-existing ignored ComfyInject extension has 463 style violations; it was left untouched as unrelated user configuration.
- Dependency audit discovery: the locked production tree currently reports 46 advisories (1 low, 27 moderate, 17 high, 1 critical). Several have no non-breaking or available fix, so an unrelated dependency migration was rejected for this configuration task and remains separate security-maintenance work.
- The shell-provided OpenAI credential failed live authentication. It was removed from the SillyTavern secret store and replaced with another already-present local OpenAI credential that passed authentication; the existing Horde credential was preserved. The secret file is mode `0600`, and neither credentials nor user settings are tracked.
- Selected the repository's ignored per-user `secrets.json` mechanism rather than adding dotenv loading. The existing `.gitignore` already excludes both `secrets.json` and `data/`, which keeps the OpenAI key and user-level extension settings out of commits.
- Voice chat is configured as the project's supported pipeline: official Speech Recognition extension for microphone capture and OpenAI transcription (`gpt-4o-transcribe`), followed by the bundled TTS extension using `gpt-4o-mini-tts`. A direct Realtime API integration was rejected because the application does not implement a Realtime client and adding one would be a broad, unnecessary feature project.
- Image generation now defaults locally to OpenAI `gpt-image-2.5-flare` for lower conversational latency. `gpt-image-2.5-sunburst` remains selectable for maximum fidelity; both aliases and their 2026-09-08 snapshots were added to the bundled model list because the checkout predated those current model IDs.
- Started OpenAI voice-chat and image-generation setup on branch `codex/chore/configure-openai-media` from the clean `release` branch. The repository had no existing decision ledger or repository-local agent instructions, so this file and `AGENTS.md` were added before application changes.
