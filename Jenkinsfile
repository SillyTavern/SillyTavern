/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('Install') {
            steps {
                echo 'Hello World!'
                sh '''

                '''
            }
        }
        stage('')
    }
}
