/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World!'
                sh '''

                '''
            }
        }
        stage('')
    }
}
