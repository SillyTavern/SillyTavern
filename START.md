# How to start

---

1. Run `setup-nginx-sillytavern.sh` to setup nginx for reverse proxy
2. Install node and PM2
3. Run `pm.sh` to start up the server.

For pushing to the repo, add the SSH private key to `~/.ssh` and run `set-git-ssh-key.sh` to configure using the `Silly Tavern Deploy` key.
